const {
  vehiclePost, readAll, readParams, readAllParams, updateVehicle, deleteVehicle,
} = require('./vehicle');

module.exports = {
  vehiclePost, readAll, readParams, readAllParams, updateVehicle, deleteVehicle,
};
